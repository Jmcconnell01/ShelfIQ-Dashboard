# ─── Streamlit Secrets ───────────────────────────────────────────────────────
# Place this file at:
#   C:\Users\jmcconnell\.streamlit\secrets.toml
#   OR in the project folder:
#   C:\Users\jmcconnell\OneDrive - Southern Crown Partners\Desktop\ShelfIQ_Streamlit\.streamlit\secrets.toml
#
# The .streamlit folder may need to be created if it doesn't exist yet.
# ─────────────────────────────────────────────────────────────────────────────

# Dashboard login password
DASHBOARD_PASSWORD = "Sharpentheedge"

# ── Google Sheets service account (for survey data sync) ─────────────────────
# Fill this in with your GCP service account JSON to enable Google Sheets sync.
# Leave as-is to run locally without Sheets (survey data saves to CSV only).
#
# [gcp_service_account]
# type = "service_account"
# project_id = "your-project-id"
# private_key_id = "your-key-id"
# private_key = "-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"
# client_email = "your-service-account@your-project.iam.gserviceaccount.com"
# client_id = "your-client-id"
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
